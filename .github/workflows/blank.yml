name: Test workflow
on:
  workflow_dispatch:
jobs:
  test1:
      runs-on: arc-runner-set
      steps:
        - name: step1
          run: echo "step1"
        - name: docker
          uses: docker://amazon/aws-cli@latest
          with: 
            args: --version
  test2:
    runs-on: arc-runner-set
    steps:
      - name: step2
        run: echo "step2"
